# Gold Layer Pipeline - Analytics and Aggregations
# Creates analytical views and aggregations from silver layer data
# Depends on silver_pipeline

resources:
  pipelines:
    gold_pipeline:
      name: retail_gold_pipeline
      catalog: ${var.catalog}
      schema: gold
      serverless: true
      libraries:
        - file:
            path: ../src/ETL/gold.py
