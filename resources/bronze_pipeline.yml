# Bronze Layer Pipeline - Raw data ingestion from ADLS
# Creates raw data tables with schema inference and tracking

resources:
  pipelines:
    bronze_pipeline:
      name: retail_bronze_pipeline
      catalog: ${var.catalog}
      schema: bronze
      serverless: true
      libraries:
        - file:
            path: ../src/ETL/bronze.py
